## 프로젝트별 경험 정리

### 1. LittleWriter
- 기간: 2024.1 ~ 2024.6
- 인원: 3명
- 역할: API 서버 개발

프로젝트 간단 요약  
생성형 AI를 활용해 동화와 삽화를 함께 생성하는 서비스입니다. LLM과 이미지 생성 모델의 불안정성을 고려해 사용자 상태 관리, 생성 파이프라인, 응답 속도 개선을 중심으로 서버 구조를 설계했습니다.

내가 한 기능 개발
- Redis 기반 사용자 진행 상태 저장 구조를 설계해 동화 생성 흐름을 상태 기반으로 관리했습니다.
- GPT와 이미지 생성 모델을 결합한 프롬프팅 파이프라인을 구현해 생성 시간을 1분에서 30초로 단축했습니다.
- `@Transactional` 내부 외부 IO를 분리해 저장 요청 응답 속도를 600ms에서 150ms로 개선했습니다.
- JPA 연관관계를 제거하고 JPQL Projection 기반 조회로 바꿔 N+1 문제를 줄였습니다.
- Redis `SETNX`를 이용해 사용자 동시 요청을 제어하고 LLM 중복 호출을 방지했습니다.

<br>

### 2. Barlow
- 기간: 2025.1 ~
- 인원: 2명
- 역할: API 서버 개발, 앱 개발 및 스토어 관리

프로젝트 간단 요약  
사용자가 관심 있는 국회 법안 정보를 받아볼 수 있는 서비스입니다. 서버에서는 인증과 AI 요약 배치 구조를 설계했고, 앱에서는 배포 자동화와 구조 개선을 함께 담당했습니다.

내가 한 기능 개발
- Credential 기반 `Authenticator` 인터페이스로 공통 인증 모듈을 설계했습니다.
- RSA 기반 암호화를 적용해 서명과 인증 책임을 분리했습니다.
- `AsyncItemProcessor`, `AsyncItemWriter` 기반 생성형 AI 요약 배치 작업을 구현했습니다.
- Polling 정책과 스레드 구조를 조정해 배치 처리 시간을 1분 이상에서 24초로 단축했습니다.
- Github Actions 기반 Play Store 자동 배포 환경을 구성했습니다.
- Feature-first 기반 Clean Architecture를 적용해 앱 구조를 개선했습니다.

<br>

### 3. Barlow Automation
- 기간: 2026.3
- 인원: 1명
- 역할: 설계 및 구현 전담

프로젝트 간단 요약  
Slack에서 기능 요청을 입력하면 AI가 GitHub 코드베이스와 기존 이슈를 분석해 초안을 만들고, 사용자 확인 후 실제 이슈까지 생성하는 agentic 시스템입니다. 핵심은 AI 호출을 붙이는 것이 아니라, 사람의 판단이 개입되는 워크플로우를 서버리스 구조로 안정적으로 운영하는 것이었습니다.

내가 한 기능 개발
- `Step Graph` 기반 상태 머신으로 Human-in-the-Loop 워크플로우를 설계했습니다.
- `WorkflowInstance`, `RESUME_MAP` 구조를 사용해 Lambda 실행 중단 이후에도 같은 문맥에서 재개되도록 구현했습니다.
- Slack retry와 사용자 동시 세션 충돌을 막기 위해 `pending-action`, `active-session` 이중 dedup 구조를 설계했습니다.
- Redis 대신 DynamoDB를 선택해 상태 저장, TTL, 조건부 쓰기, 재시도 안정성을 만족하는 서버리스 저장 구조를 만들었습니다.
- Slack의 3초 제한을 해결하기 위해 Ack Lambda와 Worker Lambda를 분리하고 SQS 기반 비동기 실행 구조를 구현했습니다.
- Ack Lambda cold start를 직접 측정하고 keep-warm 전략을 도입해 체감 지연을 1,708ms에서 466ms 수준으로 낮추는 방향을 설계했습니다.
